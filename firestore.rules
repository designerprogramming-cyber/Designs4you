rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Decoupled from Firebase Auth - Authentication is managed at the application level.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
